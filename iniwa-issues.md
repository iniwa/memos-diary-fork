# Remaining Issues

- [ ] 新規メモ投稿時に日記日付を変更してから保存できるようにする
  - 通常の新規メモ作成で、保存前に `createTime` / `updateTime` を指定できる UI を出す。
  - コメント作成には出さない。
  - 既存のカレンダー日付プリフィル (`displayTime:YYYY-MM-DD`) と編集モードの timestamp 編集は維持する。
