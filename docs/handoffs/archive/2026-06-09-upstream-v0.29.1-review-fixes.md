Read AGENTS.md, CLAUDE.md, the original upstream-update handoff, and this
review-fix handoff before editing.

The repository is currently in an active, uncommitted merge. Do not abort,
commit, push, or deploy the merge.

## Goal

Fix issues found during Codex review of the v0.29.1 integration and update
automation, then leave the corrected merge staged and ready for final review.

## Background

Codex reviewed the actual staged and unstaged diff after the first Claude Code
report.

Confirmed decisions:

- Keep the upstream `docs/superpowers/plans/` and `docs/superpowers/specs/`
  files. They are legitimate upstream v0.29.1 content and preserving the full
  upstream merge is preferred.
- Do not include the locally generated
  `server/router/frontend/dist/index.html` change. Upstream v0.29.1 did not
  change it, the existing CI rebuilds frontend assets, and the local generated
  file contains CRLF/trailing-whitespace noise.
- The restored local-only `AGENTS.md` is acceptable. Keep it untracked and do
  not modify it during this task.

Review findings that must be fixed:

1. `scripts/prepare-upstream-update.ps1` exits on real merge conflicts before
   restoring saved local-only `AGENTS.md` and `CLAUDE.md`. This can destroy or
   overwrite the operator's local files during the exact failure path where
   preservation matters most.
2. The development machine runs Windows PowerShell `5.1.26100.8521`.
   `Set-Content -Encoding utf8NoBOM` is unsupported in PowerShell 5.1, so the
   script will fail after performing the merge when updating
   `.upstream-version`.
3. The script fetches only the target tag but calculates the upstream diff from
   the tag named in `.upstream-version`. A fresh or tag-incomplete clone may not
   contain the current baseline tag, causing the diff/report step to fail.
4. `.github/workflows/upstream-release-check.yml` succeeds when a newer release
   is detected. A successful scheduled workflow with only a step summary is not
   a useful notification. Make the workflow visibly fail after writing the
   summary when a newer stable release exists.
5. `docs/upstream-update-process.md` recommends `git reset --hard HEAD`. This
   conflicts with project safety rules and is unnecessary while the no-commit
   merge is active.

## Files To Inspect

- `AGENTS.md` (read only, local only)
- `CLAUDE.md` (read only, local only)
- `docs/handoffs/2026-06-09-upstream-v0.29.1-and-update-automation.md`
- `scripts/prepare-upstream-update.ps1`
- `.github/workflows/upstream-release-check.yml`
- `docs/upstream-update-process.md`
- `docs/decisions/0002-upstream-update-policy.md`
- `server/router/frontend/dist/index.html`

## Files To Edit

- `scripts/prepare-upstream-update.ps1`
- `.github/workflows/upstream-release-check.yml`
- `docs/upstream-update-process.md`
- `docs/decisions/0002-upstream-update-policy.md` only if needed to keep it
  consistent with corrected behavior

Restore the unstaged generated change in:

- `server/router/frontend/dist/index.html`

Do not edit or stage:

- `AGENTS.md`
- `CLAUDE.md`

## Required Fixes

### PowerShell Script

- Preserve and restore local-only files on every exit path after they are
  backed up, including real merge conflicts and unexpected errors.
- Use a `try` / `finally` structure or equivalent reliable cleanup.
- Do not abort the Git merge when real conflicts occur; leave conflicts for
  manual resolution, but restore local-only files before exiting non-zero.
- Support Windows PowerShell 5.1. Write `.upstream-version` as UTF-8 without BOM
  using a PowerShell 5.1-compatible method such as
  `[System.IO.File]::WriteAllText(..., [System.Text.UTF8Encoding]::new($false))`.
- Avoid rewriting local-only files with a BOM or altered line endings.
- Ensure both the current baseline tag from `.upstream-version` and the target
  tag are available before calculating diffs. Fetch them explicitly from the
  configured upstream remote or the canonical upstream URL.
- Do not create persistent repository configuration unexpectedly. If adding an
  `upstream` remote remains part of the design, document it explicitly;
  otherwise fetch from the canonical URL directly.
- Preserve the existing safety boundary: never commit, push, deploy, or
  automatically resolve real code conflicts.

### Release Check Workflow

- Keep the useful step summary.
- When a newer stable release is detected, emit a clear annotation and exit
  non-zero so the scheduled check is visibly actionable.
- Remain read-only. Do not open issues, create branches, or write repository
  contents.

### Documentation

- Remove `git reset --hard HEAD` from rollback instructions.
- Document `git merge --abort` as the supported rollback while the prepared
  no-commit merge is active.
- Explain that a newer-release detection intentionally makes the scheduled
  workflow fail to provide visibility.
- Keep the decision document consistent with actual behavior.

### Generated Frontend Asset

- Restore `server/router/frontend/dist/index.html` to `HEAD`.
- Do not stage it.

## Constraints

- Stay on `diary-mode` and preserve the active merge.
- Do not commit, push, deploy, or abort the merge.
- Do not modify upstream v0.29.1 application changes.
- Keep upstream `docs/superpowers/` files staged.
- Do not touch secrets, `.env`, credentials, or local settings.
- Keep `AGENTS.md` and `CLAUDE.md` local-only and unstaged.
- Preserve LF line endings in edited tracked files.

## Verification

Run:

```powershell
git status --short
git diff --check
git diff --cached --check
git diff --name-only --diff-filter=U
git ls-files AGENTS.md CLAUDE.md
```

PowerShell:

- Parse `scripts/prepare-upstream-update.ps1` with Windows PowerShell 5.1.
- Confirm the script contains no `utf8NoBOM` encoding usage.
- Exercise safe guard paths where possible without changing or aborting the
  active merge.
- Reason explicitly about and report the real-conflict cleanup path.

Confirm:

- `server/router/frontend/dist/index.html` has no staged or unstaged diff.
- Newer-release workflow branch exits non-zero after writing its summary.
- `docs/superpowers/` files remain staged.
- `AGENTS.md` and `CLAUDE.md` remain untracked/ignored and unstaged.

## Expected Report

- Changed files
- Fix summary mapped to each review finding
- Verification results
- Remaining blocked checks
- Final `git status --short`
- Design questions for Codex
