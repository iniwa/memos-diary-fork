# Archived Handoffs

This directory contains completed Diary Mode handoffs.

Active handoffs, when present, are kept one level up:

```txt
docs/handoffs/
```

Use archived files for historical context only. Do not treat them as pending work unless a newer active handoff explicitly references them.
